{
  "name": "okay-hash",
  "version": "0.0.0",
  "description": "a good enough hash function",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "mocha": "1.2.x"
  },
  "scripts": {
    "test": "mocha -R spec"
  },
  "license": "BSD"
}
